extends layouts/main 
include mixins/videoBlock

block content
    div.search__header 
        h3 Searching for #{term}
    div.search__videos 
        each item in videos
            +videoBlock({
                videoFile: item.videoFile,
                title: item.title,
                views: item.views
            })